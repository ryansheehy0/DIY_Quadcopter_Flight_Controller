#include <iostream>
#include "stacked_queue.h"

using std::cout;

int main() {
	/*
	Median<int, 11> median;
	for (int i = 0; i < 11; i++) {
		median.addElement(i);
	}
	cout << "Median: " << median.getMedian() << "\n";
	*/
}